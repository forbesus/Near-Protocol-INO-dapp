import { Alert } from "react-bootstrap";

export const AddNewCollection = () => {

    return (
        <Alert variant="info" style={{marginTop: 250, textAlign: 'center'}}>
            <h1>
                Add new collection here.
            </h1>
        </Alert>
        
    )
}